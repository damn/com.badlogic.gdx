
```
lein run -m app-test
```

2D ( no graphics 2d folder )
Desktop games ( no 'backend ' )
scenes.scene2d == USER INTERFACE

tiled-maps (no 'maps')


with _tests_

everyting 'upfactored' class only used once
or another layer 'badlogic' and 'gdx' is combination layer?
so core interop pieces remain untouched
just combination layer
